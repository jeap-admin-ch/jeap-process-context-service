registry=https://registry.npmjs.org/

# Workaround: @quadrel-enterprise-ui/auth@20.x requires Angular 20, but this project uses Angular 21.
# Can be removed once the library publishes an Angular 21 compatible version.
legacy-peer-deps=true

